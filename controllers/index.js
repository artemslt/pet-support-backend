const auth = require('./auth');
const users = require('./users');
const notices = require('./notices');
const news = require('./news');
const friends = require('./friends')

module.exports = { auth, users, notices, news, friends };
